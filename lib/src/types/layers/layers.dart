// src/types/layers/layers.dart

/// Public surface of the style-layer modelling layer.
///
/// Re-exports the expression typedef, the `StyleValue<T>` sealed type, the
/// visibility enum, and the sealed `StyleLayer` hierarchy with its concrete
/// variants.
library;

export 'expression.dart';
export 'style_value.dart';
export 'layer_visibility.dart';
export 'style_layer_base.dart';