name: mapgl_2gis_web_example
description: "Demonstration application for mapgl_2gis_web package."
publish_to: 'none'

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: '>=3.47.0'

dependencies:
  flutter:
    sdk: flutter

  mapgl_2gis_web:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
